# dx-sensor プロジェクト指示書

Claude CodeはこのファイルをdX-sensorディレクトリで起動するたびに自動で読み込みます。
作業前に必ずこの内容を前提としてください。

## 基本コンセプト（最重要・変更禁止）

dx-sensorは **「定点観測した画像を時系列で比較し、変化をAIで解析する」汎用プラットフォーム** です。

- 観測対象はケースバイケースで設定可能な設計にすること。特定の対象（駐車場、ナンバープレート等）にロジックを固定しない。
- 「駐車場の空き状況チェック × ANPR」は最初のユースケースの一つに過ぎない。今後、別の観測対象（例: 施設の混雑度、屋外資材の在庫、店舗の陳列状態など）が追加される前提でコードを書くこと。
- テーブル名・関数名・型名を駐車場専用の語彙（`parking_spots`, `vehicle_events`等）に固定しすぎないよう注意する。将来的な汎用化（`observation_targets`, `observation_events`等へのリファクタ）を見据え、駐車場固有のロジックは可能な限り抽象化層の外に追い出すこと。

新しい機能や設計判断を提案する際は、「この実装は駐車場以外の観測対象にも転用できるか？」を必ず自問すること。転用できない設計を安易に採用しない。

## プロジェクトの位置づけ

- `dx-toolbox`（中小企業向け便利アプリ箱）とは**完全に独立したプロジェクト**。コードもSupabaseプロジェクトも共有しない。
- 再利用しているのはdx-toolboxで確立した**設計パターンのみ**（RLS設計、role-columnベースの権限管理）。

## アーキテクチャ上の決定事項

### マルチテナント / RLS
- 全テナントスコープのテーブルは `tenant_id` で隔離し、RLSを必ず有効化する
- 権限判定はrole列（`tenant_members.role`: owner/admin/viewer/developer）で行う。**開発者のメールアドレスをポリシーにハードコードしない**
- クロステナントアクセスが必要な開発者アカウントは `role = 'developer'` として管理する

### カメラ連携（ベンダー非依存が絶対条件）
- カメラごとの実装差異は `src/lib/cameras/` のDriver抽象化層に閉じ込める。ビジネスロジック側からベンダー固有のAPIを直接呼び出さない
- 開発環境: TP-Link Tapo（安価・入手性重視）
- 本番環境: Reolink（ONVIF安定性・FTP対応重視）
- **両ドライバーは標準ONVIF呼び出し（GetSnapshotUri, GetProfiles等）のみに依存すること。** ベンダー独自API（アプリ専用エンドポイント等）に依存すると、開発→本番の切り替えが破綻する
- カメラ切り替えは `cameras.vendor` カラムの変更のみで完結する設計を維持する（コード変更不要が原則）

### AI解析パイプライン
- 画像取得 → Storage保存 → AI解析 → 構造化データとして保存、の4段階を明確に分離する
- 解析ロジック（プロンプト、判定基準）は観測対象ごとに差し替え可能なプラグイン的構造にする（駐車場用ロジックをハードコードしない）

## フォルダー構成の規約（重要）

`src/lib/`配下は、関心事の種類ごとに以下の2系列＋横断ユーティリティで整理する。
新しいコードを追加する際は、必ずこの分類に従うこと。

- **`src/lib/sensors/`** — センサー読み込み系列。「どうやって取得するか」で分ける
  - `soracam/` — クラウドAPI直接方式（ローカルエージェント不要）
  - `balenaCloud/` — ローカルエージェント方式（Raspberry Pi + ONVIF、`agent/`ディレクトリと対）
  - 新方式を追加する際は`sensors/<方式名>/`を新設し、`sensors/README.md`の手順に従う
- **`src/lib/image-analysis/`** — 画像解析処理系列。「何を読み取るか」で分ける
  - `plate-recognizer/` — ANPR
  - `openai-vision/`, `aws-rekognition/` — 汎用画像解析（プレースホルダー）
  - センサー系列とは独立した関心事。どのセンサー系列で取得した画像にも組み合わせ可能
- **`src/lib/change-detection/`** — 特定の解析プロバイダに依存しない横断的ユーティリティ（フレーム差分判定等）

ページコンポーネントは以下のルートグループに配置する。
- `src/app/(tenant)/` — テナントユーザーが使う画面
- `src/app/(admin)/` — 開発者専用の管理画面（デバッグページ等もここに置く）
- `src/app/(auth)/` — 認証関連

API Route（`src/app/api/`配下）はページではないため上記ルートグループの対象外。
`api/cron/`（クラウド起点の定期実行）と`api/ingest/`（エージェント等からのPush受信）で分類する。

## 技術スタック
- Next.js (App Router) + TypeScript
- Supabase（RLS, Storage, Auth）
- Vercel（ホスティング、Cron）
- ONVIF準拠カメラ（開発: Tapo C110 / 本番: Reolink RLC・E1系）
- ANPR: Plate Recognizer API（駐車場ユースケースの場合のみ）

## 開発時の注意
- ナンバープレート等の個人情報に該当しうるデータを扱う場合、保存期間・アクセス制御・利用目的の掲示を意識した実装にする
- 環境変数や認証情報を平文でDBに保存しない（`cameras.secret_ref` のように参照のみ保持する）
- 新しいテーブルやAPIエンドポイントを追加する際は、既存の `tenants` / `tenant_members` / RLSパターンに揃える
